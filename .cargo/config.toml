[patch.crates-io]
serde = { git = "https://github.com/mitsuhiko/serde", branch = "format-specific-buffering" }
serde_core = { git = "https://github.com/mitsuhiko/serde", branch = "format-specific-buffering" }
serde_derive = { git = "https://github.com/mitsuhiko/serde", branch = "format-specific-buffering" }
